const image = document.getElementsByClassName('.image')
const bouton = document.getElementsByClassName('.bouton')
const imageUne = document.getElementById('image1')
const imageDeux = document.getElementById('image2')

function afficher () {
    if(image.hasClass("active")) {
        return
    } else { 
        bouton[x].classList.add(bleu)
        image[x].classList.add(active)
            }
}